#!/usr/bin/env bash
# Usage: ssh-dev user@host [ssh options]. Local config lives in zsh-ssh-terminal.
set -euo pipefail
if [[ $# -eq 0 ]]; then
  echo "Usage: $(basename "$0") user@host [ssh options]" >&2
  exit 64
fi
exec ssh -t "$@" 'exec zsh -l'
